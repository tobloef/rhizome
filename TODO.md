* Change constructor to take an object
  * `evaluator`
  * `invalidator`
  * `dependencies`
  * `errorables`
* Change the evaluator to just return the type again
* Change docs to match
* Re-write real-world usage examples